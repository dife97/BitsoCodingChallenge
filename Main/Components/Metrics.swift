import Foundation

enum Metrics { }

// MARK: - Spacings
extension Metrics {
    struct Spacings {
        static let defaultValue: CGFloat = 16
    }
}

// MARK: - Corner
extension Metrics {
    struct Radius {
        static let defaultValue: CGFloat = 8
    }
}
